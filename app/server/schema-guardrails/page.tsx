import { PageShell } from "@/components/page-shell"

export default function SchemaGuardrailsPage() {
  return <PageShell title="Schema Guardrails" description="Validating and sanitizing event payloads before sending to Meta's API." />
}
