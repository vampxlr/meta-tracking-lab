import { PageShell } from "@/components/page-shell"

export default function MissingEventIdPage() {
  return <PageShell title="Missing Event ID" description="Why event IDs are critical for deduplication and avoiding duplicate conversions." />
}
