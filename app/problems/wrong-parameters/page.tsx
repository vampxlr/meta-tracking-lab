import { PageShell } from "@/components/page-shell"

export default function WrongParametersPage() {
  return <PageShell title="Wrong Parameters" description="Common parameter errors that break event tracking and how to fix them." />
}
