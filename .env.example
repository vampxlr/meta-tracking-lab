# Meta Pixel Configuration (Client-side)
# Your Meta Pixel ID - Required for both client-side and server-side tracking
NEXT_PUBLIC_FB_PIXEL_ID=your_pixel_id_here

# Meta Conversions API Configuration (Server-side)
# System User Access Token with required permissions (ads_management, ads_read)
# Generate this in Meta Business Manager: Business Settings -> System Users
META_CAPI_ACCESS_TOKEN=your_access_token_here

# Meta Graph API Version (Optional)
# Defaults to v19.0 if not specified
# Check https://developers.facebook.com/docs/graph-api/changelog/ for latest version
META_GRAPH_API_VERSION=v19.0

# Test Event Code (Optional)
# Use this code to see events in Meta Events Manager -> Test Events tab
# Generate a code in Events Manager -> Test Events -> Generate Test Event Code
META_TEST_EVENT_CODE=
